# BRepNet C++ 使用指南

> 本文档整合了批量推理工具（CLI）和可视化工具（Qt GUI）的完整使用说明。

  

---

  

## 1. 概述



本项目提供两个工具：

  

| 工具 | 入口 | 用途 | 解决方案 |
|------|------|------|------|
| **批量推理工具** | `main_export_features.cpp` | 命令行批量处理 STEP 文件，输出 logits 和分类结果 | brepnet/brepnet.sln| 
| **可视化工具** | `visualizer/src/main.cpp` (Qt + OpenCascade) | 加载单个 STEP 文件，3D 显示预测着色，对比真实标签 | BRepNetVisualizer/BRepNetVisualizer.sln |

  

备注：加载的step文件默认路径在".\inference_data\step_files"


---

  

## 2. 批量推理工具

  

### 2.1 运行模式总览

  

| 模式 | 命令参数 | 终端输出 | 输出文件 | 适用场景 |
|------|---------|---------|---------|---------|
| **静默批量** | _(留空)_ | 仅文件名+结果 | logits + results | 所有文件批量跑 |
| **调试单文件** | `--debug --target 文件名` | 该文件全量调试 | logits + results + 中间层 | 定点调试 |
| **调试全部** | `--debug` | 所有文件全量调试 | logits + results + 中间层 | 全量检查 |
| **仅导出中间文件** | `--export` | 仅文件名+结果 | logits + results + 中间层 | 需要中间数据但不刷屏 |
| **导出指定文件** | `--export --target 文件名` | 仅文件名+结果 | logits + results + 中间层 | 只导出某文件中间数据 |
| **指定精度** | `--precision fp16` | 仅文件名+结果 | logits + results | 加载 fp16 权重推理 |
| **多线程** | `--threads 8` | 仅文件名+结果 | logits + results | 指定并行线程数 |

  

### 2.2 Visual Studio 配置

  

1. **解决方案资源管理器** → 右键项目 → **属性**

2. **配置属性** → **调试** → **命令参数**

3. 填入对应参数字符串，直接 F5 运行

  

> 修改命令参数**不需要重新编译**。

  

### 2.3 各场景详细操作

  

#### 场景 A：批量跑（日常生产）

  

**命令参数**：_(留空)_

  

```
=== BRepNet Inference Tool ===

[Model] Weights loaded successfully!

[Files] Found 28548 STEP files
  
[1/28548] step0001 -> [✓] F:12 E:18
[2/28548] step0002 -> [✓] F:8 E:12
...

All 28548 files completed!
```

  

- 不输出 `[DEBUG]`、`[Layer 0]` 等调试信息

- 不创建 `cpp_feature_maps/`、`cpp_uv_grids/` 中间文件

- 创建 `cpp_logits/*.logits` 和 `cpp_results/*.results`

  

#### 场景 B：调试单个问题文件

  

**命令参数**：`--debug --target step3268`

  

- 只对 `step3268` 输出全量调试信息和中间文件

- 其他文件保持静默

  

#### 场景 C：调试所有文件

  

**命令参数**：`--debug`

  

> ⚠️ 会产生大量终端输出和磁盘文件，仅建议少量文件（< 100）时使用。

  

#### 场景 D：只导出中间文件

  

**命令参数**：`--export`

  

终端简洁输出，但为每个文件创建中间文件。

  

#### 场景 E：只导出某个文件的中间数据

  

**命令参数**：`--export --target step3268`

  

### 2.4 参数组合速查表

  

**全部命令行参数**：

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--threads N` | 指定并行线程数 | `hardware_concurrency()`（CPU 逻辑核心数） |
| `--precision P` | 指定权重精度（fp32/fp16/bf16） | fp32 |
| `--force` | 强制重新处理，覆盖已有结果 | 关闭（跳过已处理文件） |
| `--debug` | 开启终端调试输出 | 关闭 |
| `--export` | 导出中间层特征文件 | 关闭 |
| `--target 名称` | 只处理指定文件（逗号分隔多个） | 全部文件 |

**参数组合示例**：

| 命令参数 | 终端调试 | 始终生成 | 条件生成 |
|---------|---------|---------|---------|
| _(空)_ | ❌ | ✅ logits + results | ❌ |
| `--debug` | ✅ 全部 | ✅ logits + results | ✅ 全部中间层 |
| `--debug --target X` | ✅ 仅X | ✅ logits + results | ✅ 仅X中间层 |
| `--export` | ❌ | ✅ logits + results | ✅ 全部中间层 |
| `--export --target X` | ❌ | ✅ logits + results | ✅ 仅X中间层 |
| `--precision bf16` | ❌ | ✅ logits + results | ❌ |
| `--threads 8 --force` | ❌ | ✅ logits + results | ❌ |

- **始终生成** = `cpp_logits/`、`cpp_results/`
- **条件生成** = `cpp_feature_maps/`、`cpp_uv_grids/`


  

### 2.5 target 参数说明

  

`--target` 后跟 STEP 文件的 stem 名称（不含路径和 `.step` 扩展名），大小写敏感。

  

```
# 文件路径: inference_data/step_files/20240116_231044_0_result.step

--debug --target 20240116_231044_0_result

# 多个 target 用逗号分隔（无空格）

--debug --target step3268,step5001,step9999
```

  

### 2.6 编译验证

编译完成后，请按以下步骤验证环境配置和编译结果：

**步骤 1：确保输入数据完整**

```
D:\brepnet-cplusplus\inference_data\
├── state_dict.npz        ✓ 必需（模型权重文件）
└── step_files\           ✓ 必需（至少包含一个 .step 文件）
    ├── example1.step
    ├── example2.step
    └── ...
```

如果缺少 `state_dict.npz` 或 `step_files` 目录，程序无法运行。

**步骤 2：首次运行验证（使用 Visual Studio）**

1. 在 Visual Studio 中选择 **Release | x64** 配置
2. **不输入任何命令参数**（留空），按 F5 运行
3. 查看输出，应看到：
   ```
   === BRepNet Inference Tool ===
   
   [Model] Weights loaded successfully!
   
   [Files] Found N STEP files
   [1/N] filename1 -> [✓] F:XX E:YY
   [2/N] filename2 -> [✓] F:XX E:YY
   ...
   All N files completed!
   ```

**步骤 3：检查输出文件**

编译成功后，项目根目录应生成输出目录：

```
D:\brepnet-cplusplus\
├── cpp_logits\              ✓ 始终生成（原始 logits）
│   ├── file1.logits
│   ├── file2.logits
│   └── ...
└── cpp_results\             ✓ 始终生成（分类预测结果）
    ├── file1.results
    ├── file2.results
    └── ...
```

验证这些文件是否存在且有内容：
- `.logits` 文件：每行 4 个浮点数（logits 值）
- `.results` 文件：每行包含面ID、预测类别、置信度、Top 3 类别

**步骤 4：故障检查**

如果步骤 2 或 3 失败：

| 症状 | 解决方案 |
|------|---------|
| 弹出"DLL not found"错误 | 见《环境配置.md》第十三之二章"运行时错误快速排查" |
| 无法找到 state_dict.npz | 确认文件位置：`D:\brepnet-cplusplus\inference_data\state_dict.npz` |
| 无法找到 STEP 文件 | 在 `D:\brepnet-cplusplus\inference_data\step_files\` 中放入至少一个 `.step` 文件 |
| 链接错误或编译失败 | 见《环境配置.md》第十一章"问题 0"或"问题 2" |

**至此，批量推理工具配置完成！** 可进入第 3 章学习可视化工具。

---

  

## 3. 可视化工具

  

### 3.1 功能概述

  

基于 Qt + OpenCascade 的桌面应用，支持：

  

1. 加载 STEP 文件并 3D 显示

2. 运行 BRepNet 推理，按预测类别给每个面着色（4 种颜色：chamfer/round/hole/other）

3. 导入真实标签对比，计算准确率并高亮错误面

4. 点击任意面查看序号、预测类别、真实类别

5. **人工标注功能**：导入标签并上色、修改面类别、导出标注结果

6. **工作模式系统**：预测模式和标注模式均可使用，不再互斥，支持重置

  

### 3.2 界面布局

  

窗口分三栏（左：文件侧边栏，中：3D视图，右：控制面板）：

  

| 区域 | 内容 |
|------|------|
| **左侧** | 文件浏览侧边栏（固定宽，可拖拽调整） |
| **中间** | 3D 视图（OCCT 渲染） |
| **右侧** | 控制面板 |

  

控制面板从上到下：

  

| 区域 | 控件 | 说明 |
|------|------|------|
| 文件操作 | "加载STEP文件" | 打开 `.step`/`.stp` |
| 预测操作 | "运行预测" / "导入真实标签(对比)" / "导出结果" / 准确率 | 执行推理 / 对比验证 / 导出文本 |
| 人工标注 | "手动导入标签" / "自动导入标签" / "修改当前面类别" / "导出标注结果" | 标注模式操作 |
| 重置 | "清除所有操作" | 恢复初始状态 |
| 模型信息 | 文件名/面数/当前面/悬停面 | 状态一览 |
| 结果统计 | 文本区 | 类别分布 + 错误面列表 |
| 颜色图例 | 可滚动列表 | 4 类别颜色方块（chamfer/round/hole/other） |

左侧文件浏览侧边栏从上到下：

| 控件 | 说明 |
|------|------|
| "选择目录" | 选择STEP文件所在目录，自动扫描所有 .step/.stp 文件 |
| 文件列表 | 显示文件名，**绿色粗体**表示该文件已存在同名 `.seg` 标注文件 |
| "← 上一个" / "下一个 →" | 在文件列表中快速导航（循环切换） |
| "加载选中文件" | 加载列表中选中的文件 |
| 索引显示 | `3 / 150 filename.step`，当前进度 |
| "自动导入标签" 复选框 | 勾选后加载STEP时自动导入同目录下同名的 `.seg` 标签文件（静默，不弹窗） |

双击文件列表项也可直接加载。

---
![BRepNet 界面概览](https://raw.githubusercontent.com/qiziqizi/brepnet-cplusplus/refactor/inference-only/brepnet-interface-overview.png)

---

### 3.3 鼠标操作

  

| 操作 | 功能 |
|------|------|
| 左键拖动 | 旋转视图 |
| 中键拖动 | 平移 |
| 右键单击 | 修改当前悬停面的类别（弹出4类选择对话框，可用键盘 0-3 快捷选择） |
| Ctrl + 右键单击 | 多选/取消选中面（可批量修改多个面的类别） |
| Ctrl + 右键点击空白 | 清空所有多选 |
| 滚轮 | 缩放 |
| 左键双击 | 重置视图（Fit All） |

  

**悬停面信息**：鼠标移动时，右侧"模型信息"区自动显示当前悬停面的索引、类别、真实标签和正误判断，无需点击。

  

### 3.4 基本工作流程

#### 工作流程 A：预测模式

```
① 加载STEP文件 → ② 运行预测 → ③ (可选)导入真实标签 → ④ 查看对比结果
```

  

1. **启动程序**：自动搜索并加载 `inference_data/state_dict.npz`

2. **加载STEP文件**：所有面以灰色显示

3. **运行预测**：每个面按类别着色，右侧显示统计，进入预测模式

4. **导入真实标签(对比)**（可选）：显示准确率，错误面用红色粗边框高亮

5. **点击查看**：

```
选中面: #5 | 预测: hole(2) | 真实: hole(2) ✓

选中面: #12 | 预测: chamfer(0) | 真实: round(1) ✗
```

#### 工作流程 B：人工标注模式

```
① 加载STEP文件 → ② 导入标签并上色 → ③ 修改面类别 → ④ 导出标注结果
```

1. **启动程序**：加载模型权重

2. **加载STEP文件**：所有面以暖色显示（便于区分相邻面）

3. **导入标签并上色**（两种方式）：
   - **自动导入标签**：自动查找STEP同目录下同名的 `.seg` 文件并导入
   - **手动导入标签**：弹出文件选择对话框，手动选择 `.seg` 文件

4. **修改面类别**：
   - **单面修改**：右键单击面 → 弹出4类选择对话框 → 点击类别或按键盘 0-3
   - **批量修改**：Ctrl+右键添加多个面到多选集合 → 右键单击 → 批量修改所有选中面

5. **导出标注结果**：保存为 `.seg` 文件，每行一个类别ID（0-3）。文件已存在时弹出替换确认

  

#### 工作流程 C：大批量标注（推荐）

```
① 选择目录 → ② 勾选自动导入标签 → ③ 加载→标注→导出→下一个（循环）
```

1. 点击左侧"选择目录"，选择STEP文件所在目录
2. 勾选底部"自动导入标签"复选框
3. 双击列表中第一个文件加载（已标注文件会显示绿色粗体）
4. 右键单击修改面类别（键盘 0-3 快捷选择）
5. 点击"导出标注结果"保存 `.seg`
6. 点击"下一个 →"直接加载下一个文件（自动导入并跳过已标注文件）
7. 重复 4-6 直到完成所有文件

### 3.5 导出预测结果

  

点击"导出结果"保存为 `.txt`：

  

```
BRepNet 预测结果
================
文件: D:/path/to/part001.step

面数: 6

面索引 类别ID 类别名称
------ ------ --------
0 3 other
1 2 hole
2 0 chamfer
3 1 round
4 3 other
5 0 chamfer

类别分布统计
============
chamfer: 2
round: 1
hole: 1
other: 2
```

  

### 3.6 颜色对照表

  

| ID | 类别名 | 中文 | 颜色 | RGB |
|----|--------|------|------|-----|
| 0 | chamfer | 倒角 | 暗青 | (0.184, 0.310, 0.310) |
| 1 | round | 圆角 | 深粉 | (1.0, 0.078, 0.576) |
| 2 | hole | 孔 | 鞍棕 | (0.545, 0.271, 0.075) |
| 3 | other | 其他 | 中紫 | (0.482, 0.408, 0.933) |


未运行预测时显示为灰色 `(0.7, 0.7, 0.7)`。

  

### 3.7 工作模式说明

所有功能按钮在加载STEP文件后均可用，不再有互斥限制。可以随时切换操作，无需通过重置来切换模式。点击"清除所有操作"会清除所有预测和标注数据，但不会关闭STEP文件。

### 3.8 人工标注功能详解

#### 3.8.1 自动导入标签

**功能**：自动从STEP文件所在目录查找同名的 `.seg` 文件并导入

**操作步骤**：
1. 加载STEP文件
2. 点击"自动导入标签"按钮
3. 程序在STEP同目录下查找 `<step文件名>.seg`
4. 找到则自动导入并按类别着色
5. 未找到则弹出提示

**左侧侧边栏复选框模式**：勾选"自动导入标签"后，通过"加载选中文件"、"上一个"、"下一个"加载STEP时自动导入同名 `.seg`（静默执行，不弹窗）

#### 3.8.2 手动导入标签

**功能**：手动选择标签文件并按类别给每个面着色

**操作步骤**：
1. 加载STEP文件
2. 点击"手动导入标签"按钮
3. 选择标签文件（`.seg`）
4. 程序验证标签数量与面数量是否匹配
5. 自动按类别着色
6. 右侧显示标注类别分布统计

**标签文件格式**：
```
# 注释行（可选）
0
1
2
3
0
...
```
- 每行一个整数（0-3 对应 chamfer/round/hole/other）
- 支持 `#` 开头的注释行
- 行数必须等于STEP文件的面数

#### 3.8.3 修改面类别

**功能**：修改面的类别标签（支持单面和批量）

**单面修改**：
1. 右键单击3D视图中的面
2. 弹出4类选择对话框（chamfer/round/hole/other）
3. 点击按钮或按键盘 0-3 选择类别
4. 面颜色立即更新，状态栏显示修改信息

**批量修改**：
1. Ctrl + 右键单击多个面（已选面会再次点击取消）
2. 多选面用黑色粗边线高亮
3. 右键单击（无 Ctrl）→ 弹出批量修改对话框
4. 选择类别后所有选中面统一更新
5. 修改完成后自动清空多选集合

**取消多选**：Ctrl + 右键点击空白区域

**注意**：
- 修改仅影响当前会话，不会修改原始标签文件
- 需点击"导出标注结果"保存

#### 3.8.4 导出标注结果

**功能**：将当前标注结果保存为 `.seg` 标签文件

**操作步骤**：
1. 完成标注修改
2. 点击"导出标注结果"按钮
3. 默认保存到STEP文件同目录，文件名与STEP相同
4. 若文件已存在，弹出替换确认对话框（是=替换，否=取消）
5. 保存后左侧列表中该文件变为绿色粗体（表示已标注）

**导出格式**：
```
# BRepNet 人工标注结果
# 文件: D:/path/to/model.step
# 面数: 36
# 格式: 每行一个整数 (0-3)

0
1
2
3
0
...
```

#### 3.8.5 重置功能

**功能**：清除所有预测和标注数据，恢复初始状态

**操作步骤**：
1. 点击"清除所有操作"按钮
2. 弹出确认对话框
3. 确认后：
   - 清除预测结果
   - 清除标注数据
   - 清除对比标签
   - 清除多选集合
   - 恢复暖色显示
   - STEP文件保持加载状态
- 想重新开始标注
- 想清除错误的操作

---

  

## 4. 面序号与标签系统

  

> 本节内容适用于批量推理和可视化工具，两者共享相同的面序号体系。

  

### 4.1 面序号规则

  

面序号（#0, #1, #2, ...）由 OpenCascade 的 `TopExp_Explorer` 遍历顺序确定：

  

1. `STEPControl_Reader` 读取 STEP 文件得到 `TopoDS_Shape`

2. `TopExp_Explorer(shape, TopAbs_FACE)` 深度优先遍历所有面

3. 第 1 个发现的面 → **#0**，第 2 个 → **#1**，以此类推

4. **同一 STEP 文件，任何平台、任何次数运行，顺序完全一致**

  

面序号**不等于** CAD 软件中的特征编号或建模顺序。修改零件后重新导出 STEP，原有面序号可能变化。

  

### 4.2 确认面序号的方法

  

**方法 1（推荐）：可视化工具**

  

1. 加载目标 STEP 文件

2. 左键点击面，信息栏显示 `选中面: #N`

3. 逐面记录序号和类别，写入 labels 文件

  

**方法 2：Python + OCC 脚本**

  

```python
from OCP.STEPControl import STEPControl_Reader
from OCP.TopExp import TopExp_Explorer
from OCP.TopAbs import TopAbs_FACE
from OCP.TopoDS import topods
from OCP.BRepAdaptor import BRepAdaptor_Surface
from OCP.GeomAbs import GeomAbs_Plane, GeomAbs_Cylinder, GeomAbs_Cone, GeomAbs_Sphere, GeomAbs_Torus

reader = STEPControl_Reader()
reader.ReadFile("your_file.step")
reader.TransferRoots()
shape = reader.OneShape()
  
surface_type_names = {
GeomAbs_Plane: "Plane", GeomAbs_Cylinder: "Cylinder",
GeomAbs_Cone: "Cone", GeomAbs_Sphere: "Sphere", GeomAbs_Torus: "Torus",
}

  
exp = TopExp_Explorer(shape, TopAbs_FACE)
face_id = 0
while exp.More():
face = topods.Face(exp.Current())
adaptor = BRepAdaptor_Surface(face)
stype = adaptor.GetType()
print(f"Face #{face_id}: {surface_type_names.get(stype, 'Other')}")
face_id += 1
exp.Next()
print(f"\nTotal: {face_id} faces")
```

  

**方法 3：先预测再修正**

  

运行预测后在可视化工具中逐面检查，对错误面记录序号和正确类别。

  

### 4.3 三模块面顺序一致性

  

| 模块 | 遍历代码 | 容器 |
|------|---------|------|
| 可视化显示 | `StepLoader.cpp:65` | `vector<TopoDS_Face>` |
| 可视化预测 | `BRepPipeline.h:173` | `TopTools_IndexedMapOfShape` |
| 批量推理 | `BRepPipeline.h:173` | `TopTools_IndexedMapOfShape` |

  

三者都用 `TopExp_Explorer(shape, TopAbs_FACE)` 遍历，正常 STEP 文件结果完全一致。批量推理的 `.logits` 文件第 N 行（0-indexed）对应面 #N。

  

### 4.4 Labels 文件格式

  

**格式要求**：

  

| 项目 | 要求 |
|------|------|
| 扩展名 | `.labels` 或 `.txt` |
| 每行内容 | 整数 **0 ~ 3** |
| 注释 | `#` 开头的行跳过 |
| 空行 | 自动跳过 |
| 行数 | **必须等于 STEP 文件的面数** |
| 对应关系 | 第 K 个有效行对应面 #K |



**示例**：

  

```
# part001.step 的真实标签（6个面）
3
2
3
1
0
0
```

  

  

### 4.5 面序号常见问题

  

**Q: 不同 CAD 软件导出的 STEP 文件，面顺序一样吗？**

A: 不一定。必须基于实际使用的 STEP 文件确认。

  

**Q: 修改 STEP 文件后，原来的 labels 还能用吗？**

A: 不能。任何修改都可能改变面的数量或顺序，需重新构建 labels。

  

**Q: 面序号会因操作系统或编译器不同而变化吗？**

A: 不会。遍历顺序由 STEP 文件内容唯一决定。

  

**Q: 为什么有些面看起来一样但序号不同？**

A: 几何相似但拓扑上独立的面（如多个孔的内壁）有不同序号。

  

---

  
  

  

## 5. 注意事项

  

### 批量推理相关

  

- **修改命令参数不需要重新编译**，直接 F5

- **Debug/Release 配置分别设置**，切换配置时检查命令参数

- **`--target` 大小写敏感**，精确匹配 stem 名称

- **静默模式零性能损失**，调试代码被条件跳过

  

### 可视化工具相关

  

- **推理路径一致性**：可视化工具调用 `BRepNet::forward()`，批量推理手动逐层执行，两者逻辑已同步

- **面数校验**：运行预测和导入标签时均校验面数，不匹配会拒绝操作

- **模型权重路径**：自动搜索 `{exe目录}/inference_data/state_dict.npz` 和 `{exe目录}/../../../inference_data/state_dict.npz`，找不到则"运行预测"按钮禁用

  

---

  

## 6. 故障排查

 
| 问题 | 排查方向 |
|------|---------|
| 命令参数不生效 | 检查 Debug/Release 配置是否正确，检查属性页顶部的下拉框 |
| 调试输出没出现 | 确认 `--target` 文件名拼写正确（大小写敏感），确认文件存在于 `step_files/` |
| 中间文件在哪里 | `cpp_feature_maps/`、`cpp_uv_grids/`、`cpp_logits/` |
| 可视化工具预测按钮禁用 | 确保 `inference_data/state_dict.npz` 在可执行文件的相对路径下 |
| 标签导入失败 | 检查标签行数是否等于面数，每行是否为 0-3 的整数 |
| 权重精度不匹配 | 确认 `--precision` 参数与实际权重文件精度一致，否则推理结果错误但不报错 |

---

  

## 7. 权重精度支持

  

批量推理工具支持加载三种精度的模型权重：

| 精度 | 参数 | 每元素字节数 | 说明 |
|------|------|-------------|------|
| fp32 | `--precision fp32` | 4 | 默认，直接使用 |
| fp16 | `--precision fp16` | 2 | IEEE 754 半精度，加载时自动转 fp32 |
| bf16 | `--precision bf16` | 2 | Brain Float 16，加载时自动转 fp32 |

**使用方式**：在 Visual Studio 调试属性 → 命令参数中填入 `--precision bf16`，或命令行运行时指定。

**注意事项**：
- 权重加载后统一转换为 fp32 进行推理计算，不涉及混合精度推理
- 精度参数必须与实际权重文件匹配，否则程序不会报错但结果错误
- 可视化工具固定使用 fp32，不支持精度切换



