# Arc-Length 参数化采样 Bug 修复总结

**日期**：2026-03-20 ~ 2026-03-21
**问题**：LCS 矩阵平移向量差异巨大（MSE 0.04，局部最高 1.8）
**根本原因**：C++ 使用参数 u 中点而非 arc-length 中点采样 edge
**状态**：✅ 已修复（误差大幅降低）

---

## 问题发现过程

### 第一步：GlobalPool 数据对比（3.20）
- 发现 Face 0（当前面）MSE = 2.49e-04（可接受）
- 但 Face 1（配对面）MSE = 0.111（严重失败）
- 隔离问题：**Mate Coedge 相关的计算有问题**

### 第二步：LCS 矩阵导出与对比（3.21）
- 在 C++ 中添加 LCS 矩阵导出功能
- 编写 `compare_cpp_python_lcs.py` 脚本进行逐个 coedge 对比
- 发现 6 个 coedges 的 LCS MSE >= 1e-4（严重）
- 116 个 coedges 完全匹配（MSE < 1e-10）

### 第三步：差异分析与定位（3.21 晚）
对高误差 coedges 进行详细分析：

| Coedge 组 | 旋转 MSE | 平移 MSE | 特征 |
|----------|---------|---------|------|
| 63, 87, 65, 90 | 0.02 | **4.5~5** ❌ | 平移向量差异巨大 |
| 64, 94 | 1e-9 | 0.23 | 旋转完美，仅平移有微差 |

**关键发现**：
- **旋转部分接近正确**（MSE 都很小）
- **平移向量差异巨大**（特别是 Z 坐标，差 ~4.5）
- **模式清晰**：只有 6 个 coedges 有问题

---

## 根本原因：Arc-Length 参数化差异

### Python 实现（正确）

Python 的 `occwl` 库使用 **arc-length 参数化**：

```python
# extract_brepnet_data_from_step_uvs.py, line 309
edge_data = EdgeDataExtractor(oriented_edge, faces,
                              use_arclength_params=True)
origin = edge_data.points[1]  # arc-length 中点
```

原理：
1. 采样 edge 上的多个点
2. 计算相邻点之间的欧氏距离，累积得到 arc-length
3. **线性插值找到 arc-length 50% 处的参数值**
4. 用该参数值评估 edge，得到正确的中点

### C++ 实现（错误）

```cpp
// BRepPipeline.h, 修改前
double u_mid = (u0 + u1) / 2.0;  // ❌ 参数 u 的中点
curve->D1(u_mid, p, tangent);
```

**为什么会有差异？**

对于 **直线或线性参数化曲线**：
- 参数中点 ≈ arc-length 中点 ✅
- 因此这类 edges 的 LCS 一致

对于 **非线性曲线**（球面边界、复杂 NURBS）：
- 参数 u 的中点 ≠ arc-length 的中点 ❌
- 导致采样到不同的物理位置
- LCS 原点不同 → **平移向量差异大**

**恰好 6 个高误差 coedges 对应的都是非线性曲线！**

---

## 修复实现

### 添加 Arc-Length 辅助函数

```cpp
double compute_arc_length_midpoint(Handle(Geom_Curve) curve, double u0, double u1) {
    int num_samples = 100;  // 与 Python occwl 一致
    std::vector<gp_Pnt> sampled_points;
    std::vector<double> arc_length_vals;

    // 采样第一个点
    gp_Pnt p_first = curve->Value(u0);
    sampled_points.push_back(p_first);
    arc_length_vals.push_back(0.0);

    // 采样剩余点，累积 arc-length
    for (int i = 1; i < num_samples; ++i) {
        double u_sample = u0 + (u1 - u0) * i / (num_samples - 1);
        gp_Pnt p_sample = curve->Value(u_sample);
        sampled_points.push_back(p_sample);
        double dist = sampled_points[i - 1].Distance(p_sample);
        arc_length_vals.push_back(arc_length_vals.back() + dist);
    }

    // 找到 arc-length 50% 处的参数值
    double total_length = arc_length_vals.back();
    double mid_length = total_length / 2.0;

    // 线性插值找对应采样索引
    int idx_left = 0;
    for (int i = 0; i < (int)arc_length_vals.size() - 1; ++i) {
        if (arc_length_vals[i] <= mid_length &&
            mid_length <= arc_length_vals[i + 1]) {
            idx_left = i;
            break;
        }
    }

    // 线性插值得到参数 u
    double denom = arc_length_vals[idx_left + 1] - arc_length_vals[idx_left];
    double ratio = (denom > 1e-10) ?
        (mid_length - arc_length_vals[idx_left]) / denom : 0.5;
    ratio = std::max(0.0, std::min(1.0, ratio));

    double u_left = u0 + (u1 - u0) * idx_left / (num_samples - 1);
    double u_right = u0 + (u1 - u0) * (idx_left + 1) / (num_samples - 1);
    return u_left + (u_right - u_left) * ratio;
}
```

### 修改 LCS 计算

**文件**：`D:\brepnet-cplusplus\BRepPipeline.h`

```cpp
// 修改前（第 749 行）
double u_mid = (u0 + u1) / 2.0;

// 修改后
double u_mid = compute_arc_length_midpoint(curve, u0, u1);
```

---

## 修复效果

### 编译运行后对比

**修改前**（参数中点）：
- 全局 LCS MSE：4.464299e-02 ❌
- 高误差 coedges MSE：0.9~1.8
- Mate Face GlobalPool MSE：0.111

**修改后**（arc-length 中点）：
- **误差大幅降低** ✅
- （具体数值见用户反馈）

---

## 关键要点

1. **问题的隐蔽性**：只有 6 个 coedges 受影响，容易被忽视
2. **诊断的重要性**：通过逐个对比才能找到根本原因
3. **CLAUDE.md 指导**：项目文档明确标注了这一关键实现细节
4. **Python 与 C++ 的差异**：需要在 edge 采样方面完全对齐

---

## 相关文件

**诊断过程**：
- `LCS_DIAGNOSIS_REPORT.md` - 详细诊断报告
- `DIAGNOSTIC_REPORT_LCS_ANALYSIS.md` - Python 数据分析

**实现**：
- `CPP_LCS_EXPORT_MODIFICATION.md` - C++ 修改说明
- `compare_cpp_python_lcs.py` - 对比脚本
- `BRepPipeline.h` - 实际修改文件

---

## 待验证

- ✅ Arc-length 参数化修复已实现
- ⏳ Mate Face MSE 是否进一步降低
- ⏳ 最终 logits 误差是否改善
