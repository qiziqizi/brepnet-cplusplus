# BRepNet C++ 环境配置完整指南

操作系统：Windows 10/11

本文档包含两个工具的完整环境配置：
1. **批量推理工具** (`brepnet/brepnet.sln`) - 命令行批量处理 STEP 文件
2. **可视化工具** (`BRepNetVisualizer/BRepNetVisualizer.sln`) - Qt GUI 交互式预测

---

## 一、下载并安装 Visual Studio 2022

**下载链接**：https://aka.ms/vs/17/release/vs_community.exe

**安装教程**：
- https://blog.csdn.net/InnerPeaceHQ/article/details/121716088
- https://blog.csdn.net/hw5230/article/details/129027542

**必需工作负载**：
- ✅ Desktop development with C++
- ✅ MSVC v143（或最新版）C++ compiler

---

## 二、下载并配置 CMake

CMake 是跨平台自动化构建工具，用于 C++ 代码的编译、测试和打包。

### 2.1 下载与安装

**下载**：https://cmake.org/download/
- 推荐版本：CMake 3.16 或更高
- Windows 版本：`cmake-x.x.x-windows-x86_64.msi`

**安装步骤**：
1. 运行 MSI 安装器
2. ✅ 勾选 "Add CMake to the system PATH for all users"
3. 完成安装

### 2.2 验证安装

打开 `cmd` 或 `PowerShell`，执行：
```bash
cmake --version
```

输出示例：
```
cmake version 3.28.1
```

### 2.3 其他参考

CMake 完整配置教程：https://blog.csdn.net/Natsuago/article/details/145662289

---

## 三、下载并配置 OpenCASCADE 7.9.1

OpenCASCADE 是开源 CAD 内核，用于读取和处理 STEP 文件。

### 3.1 下载 OCCT 源码

**下载地址**：https://dev.opencascade.org/release/previous

1. 找到版本 "7.9.1"（或 7.9.x 最新版）
2. 点击下载源码（Source）
3. 注意：需要使用 Google 或 GitHub 账号登录

### 3.2 创建文件夹结构

建议创建以下目录结构（注意路径，后续配置会引用）：
```
D:\
├── OCCT\                           # OpenCASCADE 主目录
│   ├── OCCT-7_9_1-Win64\           # 解压的源码目录
│   │   ├── src\
│   │   ├── adm\
│   │   └── ...
│   ├── 3rdparty-vc14-64\           # 第三方库目录（cmake 自动下载）
│   │   ├── freeimage-x.x.x-x64\
│   │   ├── freetype-x.x.x-x64\
│   │   └── ...
│   └── cmake\                       # CMake 生成目录（后续生成）
```

### 3.3 第三方库下载

OCCT 依赖多个第三方库（freeimage、freetype、tcltk 等）。有两种获取方式：

#### 方式 1：CMake 自动下载（推荐，默认方式）

**什么时候自动下载**：
在执行 `cmake .. -G "Visual Studio 17 2022" -A x64` 配置命令时，CMake 会：
1. 检测系统中是否已有第三方库
2. 如果缺失，自动从 OpenCASCADE 官方源下载
3. 解压并配置到 `D:\OCCT\3rdparty-vc14-64\` 目录

**前置条件**：
- ✅ 确保网络畅通（需要访问 OpenCASCADE 官方服务器）
- ✅ 首次配置时会下载约 100-300 MB 数据，需要 5-15 分钟

**操作步骤**：
直接进入 3.4 节执行 CMake 配置命令即可，CMake 会自动处理下载。

**下载过程输出示例**：
```
-- Downloading freeimage-3.18.0-vc14-64.zip...
-- Extracting freeimage-3.18.0-vc14-64.zip...
-- Downloading freetype-2.13.3-vc14-64.zip...
-- Extracting freetype-2.13.3-vc14-64.zip...
-- Downloading tcltk-8.6.15-vc14-64.zip...
...
```

**如果下载失败**：
查看 CMake 输出的错误信息，常见原因：
- 网络连接问题 → 使用 VPN 或稍后重试
- 下载链接失效 → 使用方式 2 手动下载或使用 conda

#### 方式 2：手动下载或使用 conda（备选方案）

**使用 conda 安装**（如果已有 Anaconda/Miniconda）：
```bash
conda install -c conda-forge freeimage freetype tcl tk
```

**手动下载**：
访问 OpenCASCADE 第三方库页面下载对应版本的压缩包，解压到 `D:\OCCT\3rdparty-vc14-64\`。

> **注意**：手动下载或使用 conda 后，需要在 CMake 配置时通过 `-D3RDPARTY_DIR` 参数指定路径。大多数情况下，直接使用 CMake 自动下载更简单。

### 3.4 通过 CMake 配置

#### 方法 1：使用 CMake GUI（简单）

1. 启动 `CMake (cmake-gui)`
2. **Where is the source code**：选择 `D:\OCCT\OCCT-7_9_1-Win64`
3. **Where to build the binaries**：选择 `D:\OCCT\cmake`（新建目录）
4. 点击 **Configure**：
   - Generator：`Visual Studio 17 2022`
   - Platform：`x64`
5. 等待初始配置完成
6. 根据提示设置必要的选项，建议保持默认
7. 再次点击 **Configure**（如有更改）
8. 点击 **Generate** 生成 Visual Studio 项目

#### 方法 2：使用命令行（快速）

打开 `cmd`，执行：
```bash
cd D:\OCCT

# 创建构建目录
mkdir cmake
cd cmake

# 运行 CMake 配置
cmake .. -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=Release

# 构建 OCCT
cmake --build . --config Release -j 8
```

### 3.5 编译 OCCT

使用 CMake 生成的 Visual Studio 解决方案 `D:\OCCT\cmake\OpenCASCADE.sln`：

1. 用 Visual Studio 2022 打开 `OpenCASCADE.sln`
2. 选择配置：**Release | x64**
3. 右键项目 → **Build Solution**
4. 等待编译完成（约 10-30 分钟）

**编译产物位置**：

⚠️ **重要提示**：不同 OCCT 版本的编译产物位置可能不同！

**传统路径**（OCCT 7.7 及更早版本）：
- 头文件：`D:\OCCT\cmake\install\include`
- 库文件：`D:\OCCT\cmake\install\lib`
- 二进制：`D:\OCCT\cmake\install\bin`

**新版本路径**（OCCT 7.8+ 或使用 vcpkg 构建）：
- 头文件：`D:\OCCT\build\inc`
- 库文件：`D:\OCCT\build\win64\vc14\lib`
- 二进制：`D:\OCCT\build\win64\vc14\bin`

> 💡 **如何确认**：编译后，在 OCCT 目录下搜索 `TKernel.lib` 文件，找到它所在的实际路径，后续配置使用该路径。

### 3.6 完整配置教程

官方或社区教程：
- https://blog.csdn.net/m0_50910915/article/details/133385412
- https://dev.opencascade.org/resources/build-instruction

---

## 四、下载并配置 Zlib

Zlib 是压缩库，cnpy 依赖它。

### 4.1 下载 Zlib

**GitHub 下载**：https://github.com/madler/zlib/releases

推荐最新版本（如 v1.3.x）：
- 源码压缩包：`zlib-x.x.x.zip`

### 4.2 编译 Zlib

1. 解压到 `D:\zlib-x.x.x\`
2. 创建构建目录：`D:\zlib-x.x.x\build`
3. 使用 CMake 配置和编译：
```bash
cd D:\zlib-x.x.x\build

cmake .. -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=Release

cmake --build . --config Release
```

**输出位置**：
- 库文件：`D:\zlib-x.x.x\build\Release\zlibstatic.lib`

### 4.3 参考教程

- https://blog.csdn.net/m0_64206188/article/details/129903489

---

## 五、下载并配置 cnpy

cnpy 用于读取和写入 NumPy 的 `.npy` 和 `.npz` 文件（模型权重文件格式）。

### 5.1 下载 cnpy

**GitHub 下载**：https://github.com/rogersce/cnpy

克隆或下载源码到 `D:\cnpy\`

### 5.2 编译 cnpy

```bash
cd D:\cnpy

mkdir build
cd build

# 配置（指定 zlib 位置）
cmake .. -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=Release -DZLIB_LIBRARY="D:\zlib-x.x.x\build\Release\zlibstatic.lib" -DZLIB_INCLUDE_DIR="D:\zlib-x.x.x"

# 构建
cmake --build . --config Release
```

**输出位置**：
- 库文件：`D:\cnpy\build\Release\cnpy.lib`
- 头文件：`D:\cnpy\include\cnpy.h`

### 5.3 参考教程

- https://blog.csdn.net/gitblog_09449/article/details/142230016

---

## 六、配置批量推理工具（brepnet.sln）

### 6.1 在 Visual Studio 中打开项目

1. 启动 Visual Studio 2022
2. **文件 → 打开 → 项目/解决方案**
3. 选择 `D:\brepnet-cplusplus\brepnet\brepnet.sln`
4. 等待项目加载完成

### 6.2 启用 C++17 支持

1. 右键项目名 → **属性**
2. **配置属性 → C/C++ → 语言**
3. **C++ 语言标准**：改为 `ISO C++17 标准 (/std:c++17)`
4. 点击 **应用 → 确定**

### 6.3 配置运行库路径

批量推理工具依赖 OpenCASCADE 的多个 DLL，需要配置系统 PATH。

⚠️ **重要**：DLL 路径因 OCCT 版本而异！

#### 方式 1：配置 Visual Studio 环境变量（推荐，调试时有效）

1. 右键项目 → **属性 → 调试 → 环境**
2. 在文本框中添加（**根据你的 OCCT 版本选择**）：

**OCCT 7.7 及更早版本**：
```
PATH=D:\OCCT\cmake\install\bin;D:\OCCT\cmake\install\bin\3rdparty;%(PATH)
```

**OCCT 7.8+ 版本**：
```
PATH=D:\OCCT\build\win64\vc14\bin;D:\OCCT\build\vcpkg_installed\x64-windows\bin;%(PATH)
```

3. 点击 **应用 → 确定**

#### 方式 2：配置系统 PATH（全局，推荐用于生产环境）

1. **Win + X** → **系统 → 高级系统设置 → 环境变量**
2. **系统变量 → Path → 编辑**
3. 根据你的 OCCT 版本新增相应路径：

**OCCT 7.7 及更早版本**：
```
D:\OCCT\cmake\install\bin
D:\OCCT\cmake\install\bin\3rdparty
```

**OCCT 7.8+ 版本**：
```
D:\OCCT\build\win64\vc14\bin
D:\OCCT\build\vcpkg_installed\x64-windows\bin
```

4. 点击 **确定** 并重启 Visual Studio 和系统（如需要）

> 💡 **如何确认路径**：在文件资源管理器中搜索 `TKernel.dll`，找到其所在目录，使用该路径。

### 6.4 配置头文件目录

1. 右键项目 → **属性**
2. **配置属性 → C/C++ → 常规 → 附加包含目录**
3. 添加以下路径（**根据你的 OCCT 版本选择**）：

**OCCT 7.7 及更早版本**：
```
D:\OCCT\cmake\install\include
D:\zlib-x.x.x\include
D:\cnpy\include
```

**OCCT 7.8+ 版本**：
```
D:\OCCT\build\inc
D:\zlib-x.x.x\include
D:\cnpy\include
```

> 💡 **如何确认路径**：在文件资源管理器中搜索 `Standard_Handle.hxx`，找到其所在目录，使用该目录。

4. 点击 **应用 → 确定**

### 6.5 配置库文件目录

⚠️ **重要**：OCCT 库文件路径因版本而异，请根据实际编译结果配置！

1. **配置属性 → VC++ 目录 → 库目录**
2. 添加以下路径（**根据你的 OCCT 版本选择**）：

**方案 A**：OCCT 7.7 及更早版本
```
D:\OCCT\cmake\install\lib
D:\zlib-x.x.x\build\Release
D:\cnpy\build\Release
```

**方案 B**：OCCT 7.8+ 或 vcpkg 构建（常见于新安装）
```
D:\OCCT\build\win64\vc14\lib
D:\zlib-x.x.x\build\Release
D:\cnpy\build\Release
```

> 💡 **如何确认路径**：在文件资源管理器中搜索 `TKernel.lib`，找到其所在目录，使用该路径。

3. 点击 **应用 → 确定**

### 6.6 配置链接库

⚠️ **重要**：OCCT 7.8+ 重构了数据交换模块，部分库被合并或重命名！

1. **配置属性 → 链接器 → 输入 → 附加依赖项**
2. 添加以下库（用分号分隔）：

**基础库**：
```
cnpy.lib
zlibstatic.lib
```

**OpenCASCADE 核心库**：

根据你的 OCCT 版本选择对应的库列表：

#### 📦 OCCT 7.8+ 版本（新版，推荐）

如果你的 OCCT 库文件在 `D:\OCCT\build\win64\vc14\lib`，使用以下列表：

```
TKernel.lib
TKMath.lib
TKBRep.lib
TKG2d.lib
TKG3d.lib
TKGeomBase.lib
TKGeomAlgo.lib
TKTopAlgo.lib
TKPrim.lib
TKBO.lib
TKBool.lib
TKShHealing.lib
TKDESTEP.lib
TKXSBase.lib
TKXSDRAWSTEP.lib
TKService.lib
TKV3d.lib
TKOpenGl.lib
TKBinL.lib
TKBinTObj.lib
TKBinXCAF.lib
TKCAF.lib
TKCDF.lib
TKDE.lib
TKDraw.lib
TKExpress.lib
TKFeat.lib
TKFillet.lib
TKLCAF.lib
TKMesh.lib
TKOffset.lib
TKOpenGlTest.lib
TKRWMesh.lib
TKStd.lib
TKStdL.lib
TKTObj.lib
TKVCAF.lib
TKXCAF.lib
TKXml.lib
TKXmlL.lib
TKXmlTObj.lib
TKXmlXCAF.lib
```

**关键变化说明**：
- ✅ `TKDESTEP.lib` 替代了 `TKSTEP.lib`（DE = Data Exchange）
- ✅ `TKXSBase.lib` 替代了 `TKSTEPBase.lib`
- ✅ `TKAIS.lib` 功能已整合到 `TKV3d.lib` + `TKService.lib`
- ❌ 删除了 `TKSTEPAttr.lib`（已整合到 TKDESTEP）
- ❌ 删除了 `TKSTEP209.lib`（已整合到 TKDESTEP）

#### 📦 OCCT 7.7 及更早版本（旧版）

如果你的 OCCT 库文件在 `D:\OCCT\cmake\install\lib`，使用以下列表：

```
TKernel.lib
TKMath.lib
TKBRep.lib
TKG2d.lib
TKG3d.lib
TKGeomBase.lib
TKGeomAlgo.lib
TKTopAlgo.lib
TKPrim.lib
TKBO.lib
TKBool.lib
TKShHealing.lib
TKSTEP.lib
TKSTEPBase.lib
TKSTEPAttr.lib
TKSTEP209.lib
TKXSBase.lib
TKService.lib
TKV3d.lib
TKAIS.lib
TKOpenGl.lib
TKBinL.lib
TKBinTObj.lib
TKBinXCAF.lib
TKCAF.lib
TKCDF.lib
TKDE.lib
TKDraw.lib
TKExpress.lib
TKFeat.lib
TKFillet.lib
TKLCAF.lib
TKMesh.lib
TKOffset.lib
TKOpenGlTest.lib
TKRWMesh.lib
TKStd.lib
TKStdL.lib
TKTObj.lib
TKVCAF.lib
TKXCAF.lib
TKXml.lib
TKXmlL.lib
TKXmlTObj.lib
TKXmlXCAF.lib
```

#### 🔧 第三方库（可选）

```
freetype.lib
FreeImage.lib
```

> **注意**：tcl86.lib 和 tk86.lib 通常不是必需的，除非你使用 OCCT 的 Draw Harness 调试工具。

3. 点击 **应用 → 确定**

> 💡 **如何确认版本**：在你的 OCCT lib 目录中查找：
> - 如果有 `TKDESTEP.lib` → 使用新版列表
> - 如果有 `TKSTEP.lib` → 使用旧版列表
> 
> 详见 `OCCT新版本库配置说明.md` 文档。

> **提示**：如果编译时遇到链接错误（LNK2019），检查库名称是否正确（可在 OCCT 安装目录 `lib` 文件夹中验证）。

### 6.7 验证配置

1. 选择配置：**Release | x64**（不建议用 Debug，性能差）
2. **构建 → 生成解决方案**（Ctrl + Shift + B）
3. 查看输出窗口，应显示 `=== Build: 1 succeeded, 0 failed ===`

### 6.8 补充 DLL 文件

编译后，程序运行需要 OpenCASCADE 的运行库 DLL 文件。

#### 自动补充（推荐）

配置了系统 PATH（见 6.3 节）后，DLL 会自动从系统 PATH 加载。

#### 手动补充

如果未配置 PATH 或需要创建独立的可执行文件包：

1. **定位 OCCT DLL 目录**（根据你的版本）：
   - OCCT 7.7 及更早：`D:\OCCT\cmake\install\bin`
   - OCCT 7.8+：`D:\OCCT\build\win64\vc14\bin`

2. 选中所有 `.dll` 文件，复制

3. **右键项目 → 在文件资源管理器中打开文件夹**

4. 向上一级到项目根目录，找到 `x64\Release\` 文件夹

5. 将 DLL 粘贴到 `x64\Release\` 中

> 💡 **vcpkg 第三方库 DLL**：如果使用 OCCT 7.8+，还需要从 `D:\OCCT\build\vcpkg_installed\x64-windows\bin` 复制第三方库 DLL（如 FreeImage.dll、freetype.dll 等）。

---

## 七、下载并安装 Qt

Qt 是 GUI 框架，仅可视化工具需要。如果只使用批量推理工具，可跳过本章。

> 推荐使用官方安装，使用conda安装的qt使用的默认编译器可能和vs的编译器不匹配

### 7.1 安装选项

#### 选项 A：使用 Anaconda（最简单）

如果已安装 Anaconda：

```bash
conda install -c conda-forge qt
```

验证：
```bash
qmake --version
```

#### 选项 B：官方 Qt 安装器（完整）

**下载地址**：https://www.qt.io/download

1. 下载 Qt Online Installer
2. 注册 Qt 账号（可免费社区版）
3. 选择安装位置：`C:\Qt\` 或 `D:\Qt\`
4. **选择组件**：
   - ✅ Qt 5.15.2 (LTS) 或 Qt 6.x
   - ✅ MSVC 2019 64-bit（匹配 Visual Studio 版本）
   - ✅ Qt Creator（可选）
5. 完成安装

### 7.2 验证安装

打开 `cmd`，执行：

```bash
qmake --version
```

输出示例（如使用 Anaconda）：
```
QMake version 5.15.2
Using Qt version 5.15.2 in D:\anaconda3\Library
```

输出示例（如使用官方 Qt）：
```
QMake version 5.15.2
Using Qt version 5.15.2 in C:\Qt\5.15.2\msvc2019_64\lib
```

### 7.3 配置到 Visual Studio（如使用官方 Qt）

#### 安装 Qt Visual Studio Tools 扩展

1. 启动 Visual Studio 2022
2. **扩展 → 管理扩展**
3. 搜索 "Qt Visual Studio Tools"
4. 下载并安装
5. 重启 Visual Studio

#### 配置 Qt 版本

1. **Qt VS Tools → Qt Versions**
2. 点击 **+** 添加新版本
3. **Version name**：任意名称（如 `Qt 5.15.2`）
4. **qmake location**：选择 qmake.exe 路径
   - 如果使用 Anaconda：`D:\anaconda3\Library\bin\qmake.exe`
   - 如果使用官方 Qt：`C:\Qt\5.15.2\msvc2019_64\bin\qmake.exe`
5. 点击 **OK**

---

## 八、配置可视化工具（BRepNetVisualizer.sln）

可视化工具基于 Qt + OpenCASCADE，需要额外的 Qt 配置。有两种构建方式：

### 方式 1：Visual Studio 直接构建（推荐）

#### 步骤 1：打开解决方案

1. **文件 → 打开 → 项目/解决方案**
2. 选择 `D:\brepnet-cplusplus\BRepNetVisualizer\BRepNetVisualizer.sln`

#### 步骤 2：配置 Qt 支持

1. 右键项目 `BRepNetVisualizer` → **Qt Project Settings**
2. ✅ 勾选 **This is a Qt project**
3. 点击 **OK**

#### 步骤 3：配置 Qt 模块

1. 右键项目 → **Qt Project Settings → Qt Modules**
2. ✅ 勾选：
   - Core
   - Gui
   - Widgets
3. 点击 **OK**

#### 步骤 4：配置项目属性

按照第六章的步骤配置 C/C++ 和链接器，注意添加 **Qt 库**：

**C/C++ → 常规 → 附加包含目录**：
```
D:\brepnet-cplusplus
D:\brepnet-cplusplus\visualizer\src
D:\OCCT\cmake\install\include
D:\zlib-x.x.x\include
D:\cnpy\include
```

如果使用 Anaconda Qt：
```
D:\anaconda3\Library\include\qt
```

如果使用官方 Qt（调整版本号）：
```
C:\Qt\5.15.2\msvc2019_64\include
```

**链接器 → 常规 → 附加库目录**：

如果使用 Anaconda Qt：
```
D:\anaconda3\Library\lib
D:\OCCT\cmake\install\lib
D:\zlib-x.x.x\build\Release
D:\cnpy\build\Release
```

如果使用官方 Qt：
```
C:\Qt\5.15.2\msvc2019_64\lib
D:\OCCT\cmake\install\lib
D:\zlib-x.x.x\build\Release
D:\cnpy\build\Release
```

**链接器 → 输入 → 附加依赖项**：

**Qt 库**：
```
Qt5Core.lib
Qt5Gui.lib
Qt5Widgets.lib
```

**OpenCASCADE 库**（核心）：

根据你的 OCCT 版本选择对应的库列表：

**OCCT 7.8+ 版本**：
```
TKernel.lib
TKMath.lib
TKBRep.lib
TKG2d.lib
TKG3d.lib
TKGeomBase.lib
TKGeomAlgo.lib
TKTopAlgo.lib
TKPrim.lib
TKBO.lib
TKBool.lib
TKShHealing.lib
TKDESTEP.lib
TKXSBase.lib
TKService.lib
TKV3d.lib
TKOpenGl.lib
freetype.lib
FreeImage.lib
```

**OCCT 7.7 及更早版本**：
```
TKernel.lib
TKMath.lib
TKBRep.lib
TKG2d.lib
TKG3d.lib
TKGeomBase.lib
TKGeomAlgo.lib
TKTopAlgo.lib
TKPrim.lib
TKBO.lib
TKBool.lib
TKShHealing.lib
TKSTEP.lib
TKSTEPBase.lib
TKSTEPAttr.lib
TKSTEP209.lib
TKXSBase.lib
TKService.lib
TKV3d.lib
TKAIS.lib
TKOpenGl.lib
freetype.lib
FreeImage.lib
tcl86.lib
tk86.lib
```

**其他库**：
```
cnpy.lib
zlibstatic.lib
```

#### 步骤 5：设置工作目录

1. 右键项目 → **属性 → 调试 → 工作目录**
2. 设置为：
   ```
   $(ProjectDir)\..\..\..
   ```
   （确保程序在 `D:\brepnet-cplusplus\` 目录，可以访问 `inference_data/`）

#### 步骤 6：编译

1. 选择配置：**Release | x64**
2. **构建 → 生成解决方案**
3. 查看输出窗口确认编译成功

#### 步骤 7：运行

1. **调试 → 开始执行（不调试）**（Ctrl + F5）

### 方式 2：CMake + Visual Studio（替代方案）

如果遇到 Qt 集成问题，可使用 CMake：

#### 步骤 1：修改 CMakeLists.txt

打开 `visualizer/CMakeLists.txt`，在文件开头添加 Qt 路径：

**如果使用 Anaconda Qt**：
```cmake
set(CMAKE_PREFIX_PATH "D:/anaconda3/Library")
```

**如果使用官方 Qt**（调整版本号）：
```cmake
set(CMAKE_PREFIX_PATH "C:/Qt/5.15.2/msvc2019_64")
```

#### 步骤 2：运行 CMake

```bash
cd D:\brepnet-cplusplus\BRepNetVisualizer

# 创建构建目录
mkdir build
cd build

# 配置（使用 Visual Studio Generator）
cmake .. -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=Release

# 构建
cmake --build . --config Release -j 8
```

#### 步骤 3：运行

生成的可执行文件位置：
```
D:\brepnet-cplusplus\BRepNetVisualizer\build\Release\BRepNetVisualizer.exe
```

---

## 九、补充 Qt DLL 文件

可视化工具运行时需要 Qt 的 DLL 文件。

### 自动补充（推荐）

配置了系统 PATH 后，Windows 会自动加载 DLL。

### 手动补充

如果运行时提示找不到 DLL：

1. **定位 Qt 的 bin 目录**：
   - Anaconda：`D:\anaconda3\Library\bin`
   - 官方 Qt：`C:\Qt\5.15.2\msvc2019_64\bin`

2. **使用 windeployqt 工具自动部署 DLL**：

```bash
cd D:\brepnet-cplusplus\BRepNetVisualizer\x64\Release

# 假设 Qt 在 Anaconda 中
D:\anaconda3\Library\bin\windeployqt.exe BRepNetVisualizer.exe

# 或官方 Qt 路径
C:\Qt\5.15.2\msvc2019_64\bin\windeployqt.exe BRepNetVisualizer.exe
```

此命令会自动复制所有必要的 DLL 到 exe 所在目录。

---

## 十、验证两个工具

### 验证批量推理工具

1. 编译完成后，在 Visual Studio 中运行或直接执行 exe
2. 应输出类似信息：
   ```
   === BRepNet Inference Tool ===
   [Model] Weights loaded successfully!
   [Files] Found N STEP files
   ```

### 验证可视化工具

1. 编译完成后，在 Visual Studio 中运行或直接执行 exe
2. 应出现 Qt 窗口，显示：
   - 左侧：3D 视图区域
   - 右侧：控制面板

---

## 十一、常见问题与排查

### 问题 0：OpenCASCADE 编译失败或卡住

**常见症状**：
- CMake Configure 或 Build 失败
- 提示缺少第三方库
- 编译进度卡住（超过 1 小时）

**排查步骤**：

1. **确保网络畅通**：CMake 需要下载第三方库（freeimage、freetype、tcltk 等），网络问题是常见原因

2. **检查 CMake 安装**：
   ```bash
   cmake --version
   ```
   应返回版本号，如 `cmake version 3.28.1`

3. **清理并重新配置**：
   ```bash
   # 删除构建目录
   rmdir /s /q D:\OCCT\cmake
   
   # 重新创建并配置
   mkdir D:\OCCT\cmake
   cd D:\OCCT\cmake
   cmake .. -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=Release
   ```

4. **如果仍失败，跳过第三方库构建**（如果已通过 conda 安装）：
   ```bash
   cmake .. -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=Release -DBUILD_3RDPARTY=OFF
   ```

5. **检查构建输出**：构建失败时，查看 CMake 或 Visual Studio 的输出窗口，找到具体错误信息，例如：
   - `file not found`：检查网络，重新配置
   - `compiler not found`：检查 Visual Studio 是否安装了 C++ 工具集
   - `insufficient disk space`：清理磁盘空间

6. **验证编译产物**：编译成功后应存在以下目录（根据版本）：
   
   **OCCT 7.7 及更早版本**：
   - 头文件：`D:\OCCT\cmake\install\include`
   - 库文件：`D:\OCCT\cmake\install\lib`
   - 二进制和 DLL：`D:\OCCT\cmake\install\bin`
   
   **OCCT 7.8+ 版本**：
   - 头文件：`D:\OCCT\build\inc`
   - 库文件：`D:\OCCT\build\win64\vc14\lib`
   - 二进制和 DLL：`D:\OCCT\build\win64\vc14\bin`
   
   在文件资源管理器中搜索 `TKernel.lib` 确认实际路径。

---

### 问题 1：找不到 OCCT 头文件

**症状**：编译错误 `fatal error C1083: Cannot open include file: 'Standard_Handle.hxx'`

**排查**：
1. **检查 OCCT 是否正确编译**：
   - OCCT 7.7 及更早：应有 `D:\OCCT\cmake\install\include` 目录
   - OCCT 7.8+：应有 `D:\OCCT\build\inc` 目录
   - 在文件资源管理器中搜索 `Standard_Handle.hxx`，确认实际路径

2. **确认头文件目录配置正确**：
   - 右键项目 → 属性 → C/C++ → 常规 → 附加包含目录
   - 检查路径是否与实际编译产物位置一致

3. **重新配置项目属性**（见第 6.4 节）

> 💡 **快速诊断**：在 Visual Studio 输出窗口中查看完整错误信息，确认项目正在搜索哪些路径。

### 问题 2：链接错误 LNK2019

**症状**：`error LNK2019: unresolved external symbol`

**常见原因与解决方案**：

1. **库文件路径不正确**：
   - OCCT 7.7 及更早：`D:\OCCT\cmake\install\lib`
   - OCCT 7.8+：`D:\OCCT\build\win64\vc14\lib`
   - 在文件资源管理器中搜索 `TKernel.lib`，使用找到的实际路径

2. **库名称错误（OCCT 7.8+ 版本差异）**：
   - 如果错误提示缺少 `TKSTEP.lib`：改用 `TKDESTEP.lib`
   - 如果错误提示缺少 `TKSTEPBase.lib`：改用 `TKXSBase.lib`
   - 如果错误提示缺少 `TKAIS.lib`：删除它（功能已在 `TKV3d.lib` 中）
   - 详见 `OCCT新版本库配置说明.md`

3. **确保 Release/Debug 配置一致**：
   - 检查项目配置是否为 **Release | x64**
   - 确认库文件也是 Release 版本

4. **清理并重新生成**：
   - 构建 → 清理解决方案
   - 构建 → 重新生成解决方案

> 💡 **快速定位**：查看链接器错误信息中的库名称，与 OCCT lib 目录中的实际文件名对比。

### 问题 3：运行时找不到 DLL

**症状**：弹出 "xxx.dll not found" 对话框（如 `TKernel.dll`、`FreeImage.dll` 等）

**排查步骤**：

1. **定位 DLL 文件位置**：
   - OCCT 7.7 及更早：
     - OCCT DLL：`D:\OCCT\cmake\install\bin`
     - 第三方 DLL：`D:\OCCT\cmake\install\bin\3rdparty`
   - OCCT 7.8+：
     - OCCT DLL：`D:\OCCT\build\win64\vc14\bin`
     - 第三方 DLL：`D:\OCCT\build\vcpkg_installed\x64-windows\bin`

2. **配置系统 PATH**（推荐）：
   - Win + X → 系统 → 环境变量
   - 将上述 DLL 目录添加到系统 PATH
   - **重启电脑**生效

3. **或手动复制 DLL**（临时方案）：
   - 从上述目录复制所有 `.dll` 文件
   - 粘贴到 `D:\brepnet-cplusplus\brepnet\x64\Release\` 目录

4. **Qt DLL**（仅可视化工具）：
   - 使用 `windeployqt` 工具自动部署（见第九章）

> 💡 **验证 PATH 配置**：打开新的 cmd 窗口，输入 `echo %PATH%`，检查 OCCT bin 目录是否在列表中。

### 问题 4：Qt 相关编译错误

**症状**：`fatal error C1083: Cannot open include file: 'QtWidgets/QApplication'` 或 MOC 错误

**排查**：
1. 确认 Qt 正确安装并配置到 Visual Studio
2. 确认 Qt Project Settings 已启用
3. 右键 `.h` 文件 → **Qt → Run moc**（手动运行 MOC 编译器）
4. 清理解决方案，重新构建

### 问题 5：CMake 找不到 Qt

**症状**：`CMake Error at CMakeLists.txt: Could not find Qt5`

**排查**：
1. 检查 `CMAKE_PREFIX_PATH` 是否正确
2. 确认 Qt 已安装
3. 尝试设置环境变量：
   ```bash
   set CMAKE_PREFIX_PATH=D:\anaconda3\Library
   ```
4. 重新运行 CMake 配置

### 问题 6：两个工具同时编译冲突

**症状**：编译一个工具时出现引用错误

**排查**：
1. 确保打开的是正确的 `.sln` 文件
2. **构建 → 清理解决方案**，然后重新构建
3. 删除 `x64/` 目录下的中间文件，重新构建

---

## 十二、环境配置速查表

| 组件 | 版本 | 下载链接 | 关键路径 |
|------|------|---------|---------|
| Visual Studio | 2022 Community | https://aka.ms/vs/17/release/vs_community.exe | 需要 Desktop development with C++ |
| CMake | 3.16+ | https://cmake.org/download/ | 添加到 PATH |
| OpenCASCADE | 7.8 ~ 7.9.1 | https://dev.opencascade.org/release/previous | 见下文版本差异 ⚠️ |
| Zlib | 1.3.x | https://github.com/madler/zlib/releases | `D:\zlib-x.x.x\build\Release` |
| cnpy | latest | https://github.com/rogersce/cnpy | `D:\cnpy\build\Release` |
| Qt | 5.15.2 或 6.x | https://www.qt.io/download 或 conda | `C:\Qt` 或 `D:\anaconda3\Library` |

### ⚠️ OCCT 版本差异速查

| 路径类型 | OCCT 7.7 及更早 | OCCT 7.8+ |
|---------|----------------|-----------|
| **头文件** | `D:\OCCT\cmake\install\include` | `D:\OCCT\build\inc` |
| **库文件** | `D:\OCCT\cmake\install\lib` | `D:\OCCT\build\win64\vc14\lib` |
| **DLL 文件** | `D:\OCCT\cmake\install\bin` | `D:\OCCT\build\win64\vc14\bin` |
| **第三方 DLL** | `D:\OCCT\cmake\install\bin\3rdparty` | `D:\OCCT\build\vcpkg_installed\x64-windows\bin` |

### 🔗 OCCT 库名称变化

| 功能 | 旧版库名（7.7-） | 新版库名（7.8+） |
|------|----------------|----------------|
| STEP 数据交换 | `TKSTEP.lib` | `TKDESTEP.lib` |
| STEP 基础 | `TKSTEPBase.lib` | `TKXSBase.lib` |
| STEP 属性 | `TKSTEPAttr.lib` | ~~已整合到 TKDESTEP~~ |
| STEP AP209 | `TKSTEP209.lib` | ~~已整合到 TKDESTEP~~ |
| 交互服务 | `TKAIS.lib` | ~~已整合到 TKV3d~~ |

> 📘 详细说明见 `OCCT新版本库配置说明.md`

---

## 十三、完整配置检查清单

在编译前，确保以下项目已完成：

- [ ] Visual Studio 2022 安装，包含 Desktop development with C++
- [ ] CMake 3.16+ 安装并添加到 PATH
- [ ] OpenCASCADE 7.9.1 编译完成，头文件和库文件可访问
- [ ] Zlib 编译完成
- [ ] cnpy 编译完成
- [ ] （仅可视化工具）Qt 5.15.2+ 安装
- [ ] （仅可视化工具）Visual Studio 的 Qt VS Tools 扩展安装
- [ ] 批量推理工具：头文件目录、库目录、链接库都已配置
- [ ] 可视化工具：在批量推理工具基础上，额外配置了 Qt 相关路径
- [ ] 工作目录配置正确，可访问 `inference_data/`
- [ ] 选择 **Release | x64** 配置
- [ ] 编译成功，无链接错误

---

## 十三之二、故障诊断快速通道

### 编译失败时的快速排查

**如果出现 LNK2019 链接错误**：

1. 打开项目属性，**确认 Release | x64 配置被选中**（不要用 Debug）

2. 查看错误信息中出现的库名（如 `TKernel.lib` 或 `TKSTEP.lib`），在文件资源管理器中搜索这个库文件：
   - OCCT 7.7 及更早：应在 `D:\OCCT\cmake\install\lib\`
   - OCCT 7.8+：应在 `D:\OCCT\build\win64\vc14\lib\`

3. **如果搜索到旧库名但项目找不到**：
   - 错误提示缺少 `TKSTEP.lib` → 新版中改名为 `TKDESTEP.lib`
   - 错误提示缺少 `TKSTEPBase.lib` → 新版中改名为 `TKXSBase.lib`
   - 错误提示缺少 `TKAIS.lib` → 新版中已删除（功能在 `TKV3d.lib` 中）
   - 参考第 6.6 节更新库列表

4. **如果库文件存在但仍报错**：
   - 检查**库目录**配置是否正确
   - 属性 → VC++ 目录 → 库目录 → 确认路径与实际一致

5. **如果库文件不存在**：
   - OCCT 编译不完整，返回第三章重新编译

**如果出现头文件找不到**（如 `Standard_Handle.hxx`）：

1. 在文件资源管理器中搜索 `Standard_Handle.hxx`，确认实际位置：
   - OCCT 7.7 及更早：应在 `D:\OCCT\cmake\install\include\`
   - OCCT 7.8+：应在 `D:\OCCT\build\inc\`

2. 检查项目头文件目录配置是否与实际位置一致

3. 如果头文件不存在，OCCT 编译失败，查看问题 0 的排查步骤

### 运行时错误快速排查

**如果运行时弹出 DLL 找不到的错误**（如 `TKernel.dll not found`）：

1. **在文件资源管理器中搜索** `TKernel.dll`，确认实际位置：
   - OCCT 7.7 及更早：`D:\OCCT\cmake\install\bin`
   - OCCT 7.8+：`D:\OCCT\build\win64\vc14\bin`

2. **检查系统 PATH**：按 `Win+Pause` 打开"关于"，查看系统 PATH 是否包含上述目录

3. **如果修改了 PATH，需要重启**：
   - 关闭 Visual Studio
   - 重启电脑（或至少重启 Visual Studio）

4. **或者手动复制 DLL**（快速临时方案）：
   - 从上述 bin 目录复制所有 `.dll` 文件
   - 粘贴到：
     - 批量推理工具：`D:\brepnet-cplusplus\brepnet\x64\Release\`
     - 可视化工具：`D:\brepnet-cplusplus\BRepNetVisualizer\x64\Release\`
   - ⚠️ OCCT 7.8+ 还需从 `D:\OCCT\build\vcpkg_installed\x64-windows\bin` 复制第三方 DLL

**可视化工具缺少 Qt DLL**：

使用 `windeployqt` 工具自动部署：
```bash
cd D:\brepnet-cplusplus\BRepNetVisualizer\x64\Release

# Anaconda Qt
D:\anaconda3\Library\bin\windeployqt.exe BRepNetVisualizer.exe

# 或官方 Qt
C:\Qt\5.15.2\msvc2019_64\bin\windeployqt.exe BRepNetVisualizer.exe
```

---

## 十四、后续步骤

**批量推理工具**：见 `用户手册.md` 第 2 章

**可视化工具**：见 `用户手册.md` 第 3 章

