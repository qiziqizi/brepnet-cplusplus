# BRepNet C++ 环境配置完整指南

> 适用范围：本指南用于在**一台全新电脑上从头搭建开发环境，从而能够修改代码、编译并运行**本项目的两个工具。
> 不含便携包（`BRepNet_Portable/`）的免安装运行方式。
>
> 最后更新：2026-08-25（与实际 `.vcxproj` 配置核对一致）

操作系统：Windows 10/11（64 位）

本项目的两个工具需要两套**独立**的依赖，务必先看完「零、环境总览」再动手：

| 工具 | 解决方案路径 | 所用 OCCT | 所用 GUI |
|------|--------------|-----------|----------|
| 批量推理工具 | `brepnet\brepnet.sln` | vcpkg：OCCT **7.9.2** | 无（纯命令行） |
| 可视化工具 | `BRepNetVisualizer\BRepNetVisualizer.sln` | conda：OCCT **7.5.1** | Qt 5.15.2 |

> ⚠️ **最重要的前提**：两个 `.vcxproj` 里的头文件/库/DLL 路径都是**硬编码**的（`D:\brepnet-cplusplus`、`D:\vcpkg\...`、`D:\anaconda3\...`、`D:\cnpy`）。
> 在全新电脑上要顺利编译，请**严格按照本文档的目录结构放置**，与当前开发机保持一致；若不想用这些路径，必须逐一搜索替换 `.vcxproj` 中的路径，不推荐。

---

## 零、环境总览与目录布局

最终需要的目录结构（建议严格一致）：

```
D:\
├── brepnet-cplusplus\                 ← 整个项目源码（从原开发机整个复制过来）
│   ├── brepnet\brepnet.sln            ← 批量推理工具解决方案
│   ├── BRepNetVisualizer\BRepNetVisualizer.sln  ← 可视化工具解决方案
│   ├── visualizer\src\                ← 可视化工具源码（头文件/源文件）
│   ├── BRepNet.h / BRepPipeline.h / ...  ← 核心头文件
│   ├── main_export_features.cpp       ← 批量推理主入口
│   └── inference_data\
│       ├── state_dict.npz             ← 模型权重（Python 端导出）
│       └── step_files\                ← 待推理的 STEP 文件
│
├── vcpkg\installed\x64-windows\       ← 批量工具：vcpkg 安装的 OCCT + zlib/yaml-cpp/tbb
├── anaconda3\                         ← Anaconda3 基础环境
│   ├── Library\                       ← (base) Qt 5.15.2、qmake.exe（可视化工具的 Qt 在此）
│   └── envs\brepnet\Library\          ← conda 环境：OCCT 7.5.1 + zlib（可视化工具的 OCCT 在此）
│
└── cnpy\                              ← cnpy 源码（cnpy.h / cnpy.cpp），两个工具共用
```

---

## 一、安装 Visual Studio 2022

**下载链接**：https://aka.ms/vs/17/release/vs_community.exe

必需工作负载：
- ✅ **Desktop development with C++**
- ✅ MSVC v143 工具集（项目 `PlatformToolset` 为 v143）
- ✅ Windows 10/11 SDK

安装参考：
- https://blog.csdn.net/InnerPeaceHQ/article/details/121716088
- https://blog.csdn.net/hw5230/article/details/129027542

> 本项目不需要 CMake 从零编译 OCCT 了（OCCT 由 vcpkg / conda 提供），因此**手动安装 CMake 非必需**。`visualizer/CMakeLists.txt` 仅作为 Qt 集成的备选方案存在，可忽略。

---

## 二、安装 Anaconda3

可视化工具依赖 Anaconda：Qt（在 base 环境）和 OCCT（在自定义环境 `brepnet`）都通过 conda 提供。

1. 从官网安装 Anaconda3（或 Miniconda）：https://www.anaconda.com/download
2. 安装到 `D:\anaconda3`（或调整后修改 `.vcxproj` 路径）
3. 确认 `conda` 命令可用。

> 为什么不直接用官方 Qt？因为本项目的 Qt 库名带 `_conda` 后缀（`Qt5Core_conda.lib`），是 conda 版 Qt 特有的；官方 Qt 安装器会有同名冲突且库名不同，直接切换很可能链接失败。当前工程按 conda 方案配置。

---

## 三、安装 OpenCASCADE

两个工具用**不同版本、不同来源**的 OCCT，各自独立安装。

### 3.1 批量推理工具：vcpkg 安装 OCCT 7.9.2

1. 获取 vcpkg（若无）：
   ```bash
   git clone https://github.com/microsoft/vcpkg D:\vcpkg
   cd D:\vcpkg
   .\bootstrap-vcpkg.bat
   ```

2. 安装 OCCT 7.9.2 到 `x64-windows`（vcpkg 会自动拉取 zlib、yaml-cpp、tbb 等依赖）：
   ```bash
   D:\vcpkg\vcpkg install occt:x64-windows
   ```
   > OCCT 版本以当前开发机为准：`D:\vcpkg\installed\x64-windows\include\opencascade\Standard_Version.hxx` 中为 **7.9.2**。若 vcpkg 源已有更新版本，可指定版本或用 `--overlay-ports`（推荐直接保持 7.9.2，代码按该版本调优）。

3. 安装产物位于 `D:\vcpkg\installed\x64-windows\`：
   - 头文件：`...\include` 与 `...\include\opencascade`
   - 库文件：`...\lib`（含 `TKernel.lib`、`TKDESTEP.lib`、`zlib.lib`、`yaml-cpp.lib`、`tbb12.lib` 等）
   - DLL：`...\bin`

4. 验证：确认 `D:\vcpkg\installed\x64-windows\lib\TKDESTEP.lib` 与 `zlib.lib` 存在（新版库命名标志）。

### 3.2 可视化工具：conda 环境安装 OCCT 7.5.1

在 Anaconda 中创建一个名为 `brepnet` 的环境，并安装 **OCCT 7.5.1**（必须老版本，因为可视化工程用的是旧库名 `TKSTEP.lib`/`TKSTEPBase.lib`，只有 OCCT 7.x 旧版才有）：

```bash
# 创建环境（Python 版本不限，仅用其 Library 目录）
conda create -n brepnet python=3.10
conda activate brepnet

# 安装 occt 7.5.1（带 zlib 等依赖）
conda install -c conda-forge "occt=7.5.1"
```

安装产物位于 `D:\anaconda3\envs\brepnet\Library\`：
- 头文件：`...\include\opencascade`
- 库文件：`...\lib`（含 `TKernel.lib`、`TKSTEP.lib`、`zlib.lib`；**没有** `TKDESTEP.lib`）
- DLL：`...\bin`

> ⚠️ 若你找 7.5.1 有困难，可参考 Python 端（`D:\BRepNet_MFTRe`）使用的 occwl 对应 OCCT 版本；但必须保证 `TKSTEP.lib` 存在，否则可视化工程链接失败。

---

## 四、安装 Qt（仅可视化工具）

可视化工具只在 **base 环境（`D:\anaconda3`）** 的 Qt，版本 **5.15.2**，库文件带 `_conda` 后缀。

```bash
# 在 base 环境安装 Qt（若已装了别的包，可执行此命令补齐）
conda install -c conda-forge qt  # 令 D:\anaconda3\Library 下有 Qt5Core_conda、qmake.exe
```

验证：
```bash
D:\anaconda3\Library\bin\qmake.exe --version
# 应显示：Using Qt version 5.15.2 in D:/anaconda3/Library/lib
```

Qt VS Tools 中 `QtInstall` 名约定为 `anaconda3_Library`，`QTDIR` 指向 `D:\anaconda3\Library`（见第七章步骤 2）。

> 若 base 环境损坏，可考虑在 `brepnet` 环境里再装 Qt，但必须同步修改 `.vcxproj` 中的 Qt 路径与 `Qt5*_conda.lib` 来源，并保证库名带 `_conda` 后缀。

---

## 五、cnpy（两个工具共用）

cnpy 用于读取 `.npz` 模型权重。本项目**直接把 cnpy 源码编译进工程**（`..\..\..\cnpy\cnpy.cpp`），因此只需源码 + 头文件，不需要先生成 `cnpy.lib`。

1. 获取 cnpy：https://github.com/rogersce/cnpy ，解压到 `D:\cnpy`（保证 `D:\cnpy\cnpy.cpp` 和 `D:\cnpy\cnpy.h` 存在）。
2. cnpy 依赖 **zlib**：
   - 批量工具：已由 vcpkg 提供 `zlib.lib`（在链接列表里）
   - 可视化工具：由 conda `brepnet` 环境提供 `D:\anaconda3\envs\brepnet\Library\lib\zlib.lib`；若链接报 `zlib` 相关错误，在该工程依赖中补加 `zlib.lib`
3. 因为 cnpy 是直接编译源码，不要在链接器里单独加 `cnpy.lib`。

---

## 六、配置批量推理工具（brepnet.sln）

### 6.1 打开项目

`D:\brepnet-cplusplus\brepnet\brepnet.sln`

### 6.2 依赖核对

以下配置已在 `.vcxproj` 中设置好。若迁移后一切按本文目录放置则**无需改动**；此处列出供排查：

**C/C++ → 常规 → 附加包含目录**
```
D:\vcpkg\installed\x64-windows\include
D:\vcpkg\installed\x64-windows\include\opencascade
D:\cnpy
```

**链接器 → 常规 → 附加库目录**
```
D:\vcpkg\installed\x64-windows\lib
```

**链接器 → 输入 → 附加依赖项**（直接取自 `brepnet.vcxproj`，含 vcpkg 依赖与系统库）

基础/工具库：`yaml-cpp.lib;tbb12.lib;zlib.lib`

OpenCASCADE 新版库（按 vcxproj 顺序）：
```
TKOffset.lib;TKFeat.lib;TKXMesh.lib;TKOpenGl.lib;TKMeshVS.lib;
TKDESTEP.lib;TKDEIGES.lib;TKDESTL.lib;TKDEVRML.lib;TKDECascade.lib;
TKBinXCAF.lib;TKXmlXCAF.lib;TKDEOBJ.lib;TKDEGLTF.lib;TKDEPLY.lib;
TKFillet.lib;TKBool.lib;TKXSBase.lib;TKStd.lib;TKStdL.lib;
TKBinTObj.lib;TKXmlTObj.lib;TKTObj.lib;TKBin.lib;TKBinL.lib;
TKXml.lib;TKXmlL.lib;TKRWMesh.lib;TKDE.lib;TKXCAF.lib;TKVCAF.lib;
TKV3d.lib;TKHLR.lib;TKMesh.lib;TKService.lib;TKCAF.lib;TKBO.lib;
TKPrim.lib;TKShHealing.lib;TKTopAlgo.lib;TKGeomAlgo.lib;TKBRep.lib;
TKGeomBase.lib;TKG3d.lib;TKG2d.lib;TKMath.lib;TKLCAF.lib;TKCDF.lib;
TKernel.lib
```

系统库：`kernel32.lib;user32.lib;gdi32.lib;winspool.lib;shell32.lib;ole32.lib;oleaut32.lib;uuid.lib;comdlg32.lib;advapi32.lib`

其他正确配置：
- **C++ 语言标准**：`ISO C++17 标准 (/std:c++17)`（`LanguageStandard=stdcpp17`）
- **配置**：选择 `Release | x64`（Debug|x64 亦可用，Debug 额外开了 AVX2 `/arch:AVX2`）
- 预处理器（Release|x64）：`NDEBUG;_CONSOLE;NOMINMAX;_CRT_SECURE_NO_WARNINGS;ENABLE_VERIFICATION=1`
- 头文件（`../../BRepNet.h` 等）与源文件（`../../main_export_features.cpp`、`../../BRepUtils.cpp`）已按相对路径加入工程，依赖 `brepnet-cplusplus` 根目录文件，勿移动。

### 6.3 配置运行 DLL 并编译

1. 将 vcpkg 的 DLL 目录加入系统 PATH 或复制到 exe 目录：
   - OCCT 及第三方 DLL：`D:\vcpkg\installed\x64-windows\bin`
2. 选择 **Release | x64**，构建 → 生成解决方案（Ctrl+Shift+B）。
3. 若运行时提示缺 DLL，把 `D:\vcpkg\installed\x64-windows\bin` 加入系统 PATH（改后重启），或把示例 `TKernel.dll`、`TKDESTEP.dll` 拷到 `x64\Release\`。

### 6.4 运行

```bash
# 工作目录应位于 d:\brepnet-cplusplus（否则需用参数指定 inference_data 路径）
D:\brepnet-cplusplus\brepnet\x64\Release\brepnet.exe --threads 8
```

参考命令行参数：`--threads N`、`--precision fp32|fp16|bf16`、`--force`。输出到 `cpp_logits/`、`cpp_results/` 等目录。

---

## 七、配置可视化工具（BRepNetVisualizer.sln）

### 7.1 安装 Qt VS Tools 扩展

1. VS → **扩展 → 管理扩展**，搜索安装 **Qt Visual Studio Tools**（对应 `QtMsBuild` 机制）。
2. 重启 Visual Studio。

### 7.2 配置 Qt 版本（QtInstall = anaconda3_Library）

1. 菜单 **Qt VS Tools → Qt Versions** → 添加。
2. Version name：`anaconda3_Library`（**必须用这个名字**，工程里 `QtInstall` 引用它）
3. Path：`D:\anaconda3`（指向 Anaconda 根）
4. 确认工程右下角属性 **QtInstall** 下拉选中 `anaconda3_Library`，Qt Modules 为 `core;gui;widgets`。

> `QtInstall` 名决定 `$(QTDIR)`，本工程要求 `$(QTDIR)` = `D:\anaconda3\Library`（在该目录下有 `bin\qmake.exe` 和带 `_conda` 后缀的 Qt 库）。

### 7.3 依赖核对

以下已在 `.vcxproj` 中设置好，按本文目录放置则无需改动；列出供排查：

**C/C++ → 常规 → 附加包含目录**
```
D:\brepnet-cplusplus
D:\brepnet-cplusplus\visualizer\src
D:\anaconda3\envs\brepnet\Library\include\opencascade
D:\cnpy
$(QTDIR)\include
$(QTDIR)\include\QtCore
$(QTDIR)\include\QtGui
$(QTDIR)\include\QtWidgets
```

**链接器 → 常规 → 附加库目录**
```
D:\anaconda3\envs\brepnet\Library\lib
D:\cnpy
$(QTDIR)\lib
```

**链接器 → 输入 → 附加依赖项**（取自 `BRepNetVisualizer.vcxproj`）

Qt 库：
```
Qt5Core_conda.lib
Qt5Gui_conda.lib
Qt5Widgets_conda.lib
```

OpenCASCADE 旧版库（conda 7.5.1，注意是 `TKSTEP*` 而非 `TKDE*`）：
```
TKernel.lib;TKMath.lib;TKBRep.lib;TKG2d.lib;TKG3d.lib;
TKGeomBase.lib;TKGeomAlgo.lib;TKTopAlgo.lib;TKPrim.lib;TKBO.lib;
TKBool.lib;TKShHealing.lib;TKSTEP.lib;TKSTEPBase.lib;TKSTEPAttr.lib;
TKSTEP209.lib;TKXSBase.lib;TKService.lib;TKV3d.lib;TKOpenGl.lib
```

（若链接报 zlib 相关错误，补加 `zlib.lib`，它位于 `D:\anaconda3\envs\brepnet\Library\lib\`）

其他正确配置：
- **C++ 语言标准**：`ISO C++17 (/std:c++17)`
- **附加选项**：`/utf-8`
- 预处理器：`_CRT_SECURE_NO_WARNINGS;_WINDOWS;_UNICODE;UNICODE`
- **子系统**：Windows

### 7.4 自动复制运行的 DLL（PostBuildEvent）

工程自带了“构建后事件”，编译成功后会自动把运行时需要的 DLL 复制到输出目录，因此**运行无需手动配 PATH**。它复制：
- OCCT 及其依赖 DLL：`D:\anaconda3\envs\brepnet\Library\bin\*.dll`
- Qt DLL：`D:\anaconda3\Library\bin\Qt5Core*.dll`、`Qt5Gui*.dll`、`Qt5Widgets*.dll`、`libEGL*.dll`、`libGLESv2*.dll`
- Qt 插件：`D:\anaconda3\Library\plugins\platforms\*.dll`、`...\styles\*.dll`（复制到输出目录的 `platforms\`、`styles\`）

> 若你迁移后的 conda/Qt 路径不同，需同步修改 `.vcxproj` 的 `<PostBuildEvent>` 里的 `xcopy` 源路径。

### 7.5 编译运行

1. 选择 **Release | x64**。
2. 生成解决方案（Qt 工程会自动运行 MOC；若 `.h` 里加了带 `Q_OBJECT` 的类，右键头文件 → Qt → Run moc）。
3. **调试 → 开始执行（不调试）**，或直接运行输出目录里的 `BRepNetVisualizer.exe`。
4. 确认弹出 Qt 窗口（左：3D 视图，右：控制面板）。程序会在项目根目录访问 `inference_data/`，故工作目录/输出位置需能访问 `state_dict.npz`。

---

## 八、验证两个工具

### 批量推理工具
```bash
D:\brepnet-cplusplus\brepnet\x64\Release\brepnet.exe
```
应输出 `[Model] Weights loaded successfully!` 与扫描到的 STEP 文件数。

### 可视化工具
双击编译所得 `BRepNetVisualizer.exe`，载入 STEP 文件并点击“运行预测”，模型按类别着色（4 类：chamfer/round/hole/other）。

---

## 九、常见问题与排查

### 问题 1：找不到 OCCT 头文件（C1083: Standard_Handle.hxx）
- 批量工具：确认 `D:\vcpkg\installed\x64-windows\include\opencascade\Standard_Handle.hxx` 存在。
- 可视化工具：确认 `D:\anaconda3\envs\brepnet\Library\include\opencascade\Standard_Handle.hxx` 存在。
- 验证 vcxproj 的 include 目录是否指向上述位置。

### 问题 2：链接错误 LNK2019
- 检查实际 `.lib` 是否齐全：批量在 `D:\vcpkg\installed\x64-windows\lib`，可视化在 `D:\anaconda3\envs\brepnet\Library\lib`。
- 库名版本混淆：批量用新版 `TKDESTEP.lib`；可视化用旧版 `TKSTEP.lib`。两者不要混用。
- 确认当前配置为 **Release|x64**，避免与 Debug 库混链。
- 若提示 `yaml-cpp`/`tbb12`/`zlib` 找不到：确认 vcpkg 那三个库已装。

### 问题 3：运行时找不到 DLL
- 批量：把 `D:\vcpkg\installed\x64-windows\bin` 加入系统 PATH（重启），或复制 DLL 到 `x64\Release\`。
- 可视化：确认编译成功时 PostBuildEvent 已执行（看输出窗口是否打印“DLL 复制完成”）。缺 Qt 平台插件时 `platforms\` 目录必须存在。

### 问题 4：Qt 报错 / MOC 问题
- 确认已装 Qt VS Tools 且 `QtInstall` = `anaconda3_Library`（第七章）。
- 若改过带 `Q_OBJECT` 的头文件，重新生成解决方案触发 MOC。

### 问题 5：lib 枚举依赖但库缺失
- 批量工具新库全集过大且部分库（如 `TKBin*`、`TKXml*`）仅 IDE 生成时可能不需要，但 vcxproj 依 `D:\vcpkg\installed\x64-windows\lib` 中**确实存在的同名 lib** 为准；若 vcpkg 装的 OCCT 不带某些罕见模块，可从依赖列表移除对应 `*.lib` 再编译。

---

## 十、完整配置检查清单

- [ ] Visual Studio 2022，含 Desktop development with C++、MSVC v143、Win10/11 SDK
- [ ] Anaconda3 安装到 `D:\anaconda3`
- [ ] vcpkg 克隆到 `D:\vcpkg` 并执行 `bootstrap`
- [ ] `vcpkg install occt:x64-windows` 完成，`...\lib\TKDESTEP.lib` 与 `zlib.lib` 存在
- [ ] conda 环境 `brepnet` 创建，`occt=7.5.1` 安装完成，`...\lib\TKSTEP.lib` 存在
- [ ] base 环境 Qt 5.15.2（`D:\anaconda3\Library\bin\qmake.exe` 可用）
- [ ] cnpy 放到 `D:\cnpy`（含 `cnpy.cpp` / `cnpy.h`）
- [ ] Qt VS Tools 扩展已装，Qt Versions 里 `anaconda3_Library` 指到 `D:\anaconda3`
- [ ] 两个 `.sln` 均能以 **Release | x64** 编译通过
- [ ] 批量工具运行输出权重加载成功
- [ ] 可视化工具运行出现 Qt 窗口并能给 STEP 面着色
- [ ] `D:\brepnet-cplusplus\inference_data\state_dict.npz` 与 `step_files\` 已就位

---

## 附：当前实际版本速查表（2026-08-25 核对）

| 组件 | 版本/来源 | 位置 | 服务于 |
|------|-----------|------|--------|
| OCCT | 7.9.2 | `D:\vcpkg\installed\x64-windows` | 批量推理工具 |
| OCCT | 7.5.1 | `D:\anaconda3\envs\brepnet\Library` | 可视化工具 |
| Qt | 5.15.2 | `D:\anaconda3\Library` | 可视化工具（库名带 `_conda`） |
| zlib | vcpkg | `D:\vcpkg\installed\x64-windows\lib` | 批量工具（cnpy 依赖） |
| zlib | conda | `D:\anaconda3\envs\brepnet\Library\lib` | 可视化工具（cnpy 依赖） |
| cnpy | 源码 | `D:\cnpy` | 两个工具（直接编译 `cnpy.cpp`） |
| C++ | VS2022 / v143 / C++17 | `brepnet.sln`、`BRepNetVisualizer.sln` | 两个工具 |